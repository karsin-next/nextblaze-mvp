[phases.setup]
nixPkgs = ["nodejs-20_x", "python3", "gcc", "sqlite"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "HOSTNAME=0.0.0.0 PORT=$PORT npm run start"
